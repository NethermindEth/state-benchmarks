infrastructure:
  manage: false
  ssh_command: ""
  compose_paths:
    monitoring: "docker/docker-compose.monitoring.yml"
    clients: "docker/docker-compose.clients.yml"

nodes:
  # Replace REMOTE_HOST with your node's address; don't commit real endpoints.
  rpc_url: "http://REMOTE_HOST:8545"
  clients:
    - "remote-node"
  data_dirs:
    remote-node: "/tmp"

metrics:
  prometheus_url: "http://localhost:9090" # won't work, but aggregator handles failures gracefully
  sync_window: "1h"
  load_window: "5m"
  queries:
    cpu_percent_peak:           'max_over_time((rate(container_cpu_usage_seconds_total{name="benchmark_{client}"}[1m]) * 100)[{window}:1m])'
    cpu_percent_sustained:      'avg_over_time((rate(container_cpu_usage_seconds_total{name="benchmark_{client}"}[1m]) * 100)[{window}:1m])'
    rss_peak_bytes:             'max_over_time(container_memory_rss{name="benchmark_{client}"}[{window}])'
    rss_sustained_bytes:        'avg_over_time(container_memory_rss{name="benchmark_{client}"}[{window}])'
  client_queries: {}
  regression_threshold: 0.25
  higher_is_better:
    - "rpc_requests_sec"
    - "gas_per_sec"

load_test:
  run_time: "5m"
  users: 50
  spawn_rate: 10
  wait_time:
    min: 0.1
    max: 0.5
  # Never enable against a remote node you don't own: eth_sendTransaction
  # spends real gas and needs an unlocked account.
  gas_tests_enabled: false
  task_weights:
    eth_getBalance: 3
    eth_getStorageAt: 3
    eth_call: 2
    eth_getCode: 2
    eth_getProof: 1
    eth_sendTransaction: 1  # only used when gas_tests_enabled is true
  latency_buckets: [1, 2, 5, 10, 25, 50, 100, 250, 500, 1000, 2500, 5000]
  recent_block_window: 1000
  addresses:
    - "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045"
    - "0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2"
  slots:
    - "0x0"
    - "0x1"
    - "0x2"
  eth_call_shapes:
    - "0x18160ddd"
    - "0x313ce567"
    - "0x95d89b41"
    - "0x06fdde03"
